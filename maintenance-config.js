// Maintenance mode switch for Ambient OS.
// Set to true and push to redirect every visitor (index.html and login.html) to
// maintenance.html instead of the real site. Set back to false and push to restore it.
// To preview the live site yourself while this is true, add ?preview=1 to the URL.
window.MAINTENANCE_MODE = false;
